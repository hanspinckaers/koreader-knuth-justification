local _ = require("gettext")

return {
    fullname = _("Knuth justification"),
    description = _([[Controls paragraph-wide optimized justification in the patched CREngine reader. Settings are stored per book and can be captured by Profiles.]]),
}
