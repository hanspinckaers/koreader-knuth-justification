local _ = require("gettext")

return {
    fullname = _("Page-level Knuth justification"),
    description = _([[Optimizes line breaks against a shared word-spacing baseline for each page. Settings are stored per book and can be captured by Profiles.]]),
}
