local _ = require("gettext")

return {
    fullname = _("Page-level Knuth + fractional kerning"),
    description = _([[Page-level line breaking, fractional best kerning, and built-in Literata 16 pt comparison presets. Settings are stored per book and can be captured by Profiles.]]),
}
